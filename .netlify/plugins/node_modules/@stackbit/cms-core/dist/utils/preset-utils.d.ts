import { Preset } from '@stackbit/sdk';
import * as CSITypes from '@stackbit/types';
import { Logger, Model } from '@stackbit/types';
export declare function getPresetFromDocument({ srcType, srcProjectId, csiDocument, csiAssetMap, logger }: {
    srcType: string;
    srcProjectId: string;
    csiDocument: CSITypes.Document;
    csiAssetMap: Record<string, CSITypes.Asset>;
    logger?: Logger;
}): Preset | null;
export declare function getDocumentObjectFromPreset(preset: Preset, model?: Model): Record<string, any>;
//# sourceMappingURL=preset-utils.d.ts.map