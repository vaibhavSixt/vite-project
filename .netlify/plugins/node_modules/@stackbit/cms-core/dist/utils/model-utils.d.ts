import { Logger, ModelWithSource } from '@stackbit/types';
import { Model } from '@stackbit/sdk';
export declare function normalizeModels({ models, logger }: {
    models: ModelWithSource[];
    logger: Logger;
}): ModelWithSource[];
export declare function validateModels<T extends Model>({ models, logger }: {
    models: T[];
    logger: Logger;
}): T[];
export declare function getModelMap<T extends Model>({ models }: {
    models: T[];
}): Record<string, T>;
//# sourceMappingURL=model-utils.d.ts.map