import { SearchSchema } from './search-utils';
import { ImageModel } from '@stackbit/sdk';
import { Model, ScheduledAction } from '@stackbit/types';
import { SearchFilterItem } from '../types';
import * as ContentStoreTypes from '../types';
export type MetaFieldMethodContext = {
    locale?: string;
    schema: SearchSchema;
    activeScheduledActionsByDocumentId: Record<string, ScheduledAction[]>;
};
export type MetaFieldMethod = ({ filter, document, context }: {
    filter: SearchFilterItem;
    document: ContentStoreTypes.Document;
    context: MetaFieldMethodContext;
}) => {
    field: ContentStoreTypes.DocumentField;
    model?: Model | ImageModel;
} | {
    result: boolean;
};
export declare const META_FIELDS: Record<string, MetaFieldMethod>;
//# sourceMappingURL=meta-fields-filters.d.ts.map