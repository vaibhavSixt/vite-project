import type { ContentEnginePublicAPI } from "@netlify/content-engine";
type PreviewOpts = {
    cwd: string;
    /** Run the preview in local dev mode */
    dev: boolean;
    /** Preview the GraphQL server for your Netlify Connect plugin" */
    connector: boolean;
    /** Enable UI Preview. If this is not set it will be enabled by default */
    ui?: boolean | undefined;
    siteType: "extension-ui" | "integration-ui";
};
export type StartEngine = () => Promise<ContentEnginePublicAPI>;
export declare const preview: (opts: PreviewOpts) => Promise<{
    startEngine?: false | StartEngine;
}>;
export {};
//# sourceMappingURL=preview.d.ts.map