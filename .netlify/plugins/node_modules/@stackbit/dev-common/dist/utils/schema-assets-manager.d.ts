import { PresetMap, Model } from '@stackbit/sdk';
import * as StackbitTypes from '@stackbit/types';
export declare function handleSchemaAssets<T extends Model>({ models, presets, rootDir, stackbitYamlDir, logger, userLogger, staticThemeAssetsPublicPath, staticThemeAssetsFilePath }: {
    models?: T[];
    presets?: PresetMap;
    rootDir: string;
    stackbitYamlDir: string;
    logger: StackbitTypes.Logger;
    userLogger: StackbitTypes.Logger;
    staticThemeAssetsPublicPath: string;
    staticThemeAssetsFilePath: string;
}): Promise<{
    models: T[];
    presets: PresetMap;
}>;
//# sourceMappingURL=schema-assets-manager.d.ts.map