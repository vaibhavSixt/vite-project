import Runner from '../runner';
interface ServerOptions {
    serverPort: number;
    ssgPort: number;
    ssgHost: string;
    rootDir: string;
    runnableDir?: string;
    uuid: string;
    localDetails: any;
    cmsType?: string;
    csiEnabled?: boolean;
    csiWebhookUrl?: string;
    contentfulAccessToken?: string;
    contentfulSpaceIds?: [string];
    contentfulPreviewTokens?: [string];
}
export declare function start({ serverPort, ssgPort, ssgHost, rootDir, runnableDir, uuid, cmsType, localDetails, ...options }: ServerOptions): Promise<Runner>;
export {};
//# sourceMappingURL=index.d.ts.map