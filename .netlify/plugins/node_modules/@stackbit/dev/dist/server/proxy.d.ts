import { Router } from 'express';
import Runner from '../runner';
export default function proxyMiddleware(router: Router, ssgHost: string, ssgPort: number, runner: Runner): Router;
//# sourceMappingURL=proxy.d.ts.map