import { Router } from 'express';
import Runner from '../runner';
export default function routes(router: Router, runner: Runner, uuid: string, openUrl: string): void;
//# sourceMappingURL=routes.d.ts.map