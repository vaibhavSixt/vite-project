"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
exports.__esModule = true;
exports.printInstructions = printInstructions;
var _chalk = _interopRequireDefault(require("chalk"));
var _prepareUrls = require("./prepare-urls");
function printInstructions(program) {
  const urls = (0, _prepareUrls.prepareUrls)(program.https ? `https` : `http`, program.host, program.port);
  const appName = program.sitePackageJson.name || `(Unnamed package)`;
  console.log(`\n${appName} is ready:`);
  if (urls.lanUrlForTerminal) {
    console.log(`  ${_chalk.default.bold(`Local:`)}            ${urls.localUrlForTerminal}__graphql`);
    console.log(`  ${_chalk.default.bold(`On Your Network:`)}  ${urls.lanUrlForTerminal}__graphql\n`);
  } else {
    console.log(`  ${urls.localUrlForTerminal}__graphql\n`);
  }
}
//# sourceMappingURL=print-instructions.js.map